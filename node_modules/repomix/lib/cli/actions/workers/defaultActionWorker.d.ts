import type { RepomixConfigMerged } from '../../../config/configSchema.js';
import { type PackResult } from '../../../core/packager.js';
import type { CliOptions } from '../../types.js';
export interface DefaultActionTask {
    directories: string[];
    cwd: string;
    config: RepomixConfigMerged;
    cliOptions: CliOptions;
    stdinFilePaths?: string[];
}
export interface PingTask {
    ping: true;
}
export interface DefaultActionWorkerResult {
    packResult: PackResult;
    config: RepomixConfigMerged;
}
export interface PingResult {
    ping: true;
}
declare function defaultActionWorker(task: DefaultActionTask): Promise<DefaultActionWorkerResult>;
declare function defaultActionWorker(task: PingTask): Promise<PingResult>;
export default defaultActionWorker;
export declare const onWorkerTermination: () => Promise<void>;
