server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Redireciona todas as rotas desconhecidas para o index.html (Padrão do React)
        try_files $uri $uri/ /index.html;
    }
}